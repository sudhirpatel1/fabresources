{
  "openapi": "3.0.0",
  "info": {
    "title": "Application Insights API",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "https://api.applicationinsights.io/v1",
      "description": "Current request server"
    }
  ],
  "paths": {
    "/apps/{appId}/query": {
      "get": {
        "summary": "Query traces",
        "parameters": [
          {
            "name": "appId",
            "in": "path",
            "required": true,
            "schema": { "type": "string" }
          },
          {
            "name": "query",
            "in": "query",
            "required": true,
            "schema": { "type": "string" }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful response"
          }
        }
      }
    }
  }
}
 
